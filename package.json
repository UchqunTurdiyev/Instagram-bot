{
  "name": "instagram-bot",
  "version": "1.0.0",
  "description": "Instagram DM va comment auto-reply bot (Vercel)",
  "main": "api/webhook.js",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.7.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
